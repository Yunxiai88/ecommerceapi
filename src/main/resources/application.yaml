spring:
    datasource:
        url: jdbc:mysql://localhost:3306/ecommercedb?serverTimezone=UTC
        username: root
        password: Test123!
        driver-class-name: com.mysql.cj.jdbc.Driver

    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
        properties:
            hibernate:
                dialect: org.hibernate.dialect.MySQL55Dialect